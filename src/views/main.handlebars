<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Catalog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <style>
    :root {
      --brand: #0f62fe;
      --brand-hover: #0043ce;
      --surface: #f4f4f4;
      --text-primary: #161616;
      --text-secondary: #525252;
      --border: #e0e0e0;
    }

    body {
      background-color: #fff;
      color: var(--text-primary);
      font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    }

    /* Navbar */
    .navbar {
      border-bottom: 1px solid var(--border);
      background: #fff;
    }
    .navbar-brand {
      font-weight: 700;
      font-size: 1.25rem;
      color: var(--text-primary);
    }
    .navbar .form-control:focus {
      border-color: var(--brand);
      box-shadow: 0 0 0 0.2rem rgba(15,98,254,.15);
    }

    /* Sidebar filters */
    .filter-sidebar {
      background: #fff;
      border: 1px solid var(--border);
      border-radius: .5rem;
      position: sticky;
      top: 90px;
    }
    .filter-sidebar h6 {
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: .04em;
      font-size: .75rem;
      color: var(--text-secondary);
    }
    .filter-sidebar .form-label {
      font-size: .875rem;
      color: var(--text-primary);
      font-weight: 500;
    }
    .filter-sidebar .form-check-label {
      font-size: .875rem;
      color: var(--text-secondary);
    }
    /* Product cards */
    .product-card {
      border: 1px solid var(--border);
      border-radius: .5rem;
      transition: box-shadow .2s ease, transform .2s ease;
      overflow: hidden;
      background: #fff;
    }
    .product-card:hover {
      box-shadow: 0 4px 20px rgba(0,0,0,.08);
      transform: translateY(-2px);
    }
    .product-card img {
      width: 100%;
      height: 200px;
      object-fit: cover;
      background: var(--surface);
    }
    .product-card .card-body {
      padding: 1rem;
    }
    .product-card .card-title {
      font-weight: 600;
      font-size: 1rem;
      color: var(--text-primary);
      margin-bottom: .25rem;
    }
    .product-card .product-category {
      font-size: .75rem;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: .03em;
    }
    .product-card .product-price {
      font-weight: 700;
      font-size: 1.125rem;
      color: var(--brand);
    }

    .btn-brand {
      background-color: var(--brand);
      border-color: var(--brand);
      color: #fff;
      font-weight: 500;
      font-size: .875rem;
    }
    .btn-brand:hover {
      background-color: var(--brand-hover);
      border-color: var(--brand-hover);
      color: #fff;
    }

    .btn-outline-brand {
      border-color: var(--brand);
      color: var(--brand);
      font-weight: 500;
      font-size: .875rem;
    }
    .btn-outline-brand:hover {
      background-color: var(--brand);
      color: #fff;
    }

    .badge-category {
      background: var(--surface);
      color: var(--text-secondary);
      font-weight: 500;
      font-size: .75rem;
    }

    .price-display {
      font-weight: 600;
      font-size: .875rem;
      color: var(--text-primary);
    }

    .results-info {
      font-size: .875rem;
      color: var(--text-secondary);
    }

    #noResults {
      display: none;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg sticky-top py-3">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center gap-2" href="#">
        <i class="bi bi-bag-fill" style="color: var(--brand);"></i>
        ShopCatalog
      </a>
      <div class="d-flex align-items-center gap-3 ms-auto">
        <div class="d-none d-md-flex align-items-center gap-2" style="width: 380px;">
          <div class="position-relative flex-grow-1">
            <i class="bi bi-search position-absolute top-50 translate-middle-y ms-3 text-secondary"></i>
            <input
              type="text"
              id="searchInput"
              class="form-control ps-5"
              placeholder="Search products..."
              onkeyup="filterProducts()"
            >
          </div>
          <button class="btn btn-brand" onclick="filterProducts()">Search</button>
        </div>
        <a href="#" class="text-decoration-none position-relative" data-bs-toggle="offcanvas" data-bs-target="#cartOffcanvas" aria-controls="cartOffcanvas">
          <i class="bi bi-cart2 fs-5" style="color: var(--text-primary);"></i>
          <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="font-size:.6rem;" id="cartBadge">0</span>
        </a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container py-4">

    <!-- Mobile search (visible on small screens) -->
    <div class="d-md-none mb-3">
      <div class="d-flex align-items-center gap-2">
        <div class="position-relative flex-grow-1">
          <i class="bi bi-search position-absolute top-50 translate-middle-y ms-3 text-secondary"></i>
          <input
            type="text"
            id="searchInputMobile"
            class="form-control ps-5"
            placeholder="Search products..."
            onkeyup="syncSearch(this)"
          >
        </div>
        <button class="btn btn-brand" onclick="filterProducts()">Search</button>
      </div>
    </div>

    <div class="row g-4">

      <!-- Sidebar Filters -->
      <div class="col-lg-3">
        <div class="filter-sidebar p-3">
          <div class="d-flex align-items-center justify-content-between mb-3">
            <h5 class="mb-0 fw-bold" style="font-size:1rem;">Filters</h5>
            <button class="btn btn-sm btn-link text-decoration-none p-0" style="color: var(--brand); font-size:.8rem;" onclick="resetFilters()">
              Clear all
            </button>
          </div>

          <hr>

          <!-- Name Filter -->
          <div class="mb-4">
            <h6 class="mb-2">Name</h6>
            <input
              type="text"
              id="nameFilter"
              class="form-control form-control-sm"
              placeholder="Filter by name..."
              onkeyup="filterProducts()"
            >
          </div>

          <!-- Category Filter -->
          <div class="mb-4">
            <h6 class="mb-2">Category</h6>
            <div class="form-check mb-2">
              <input class="form-check-input" type="checkbox" value="electronics" id="catElectronics" onchange="filterProducts()">
              <label class="form-check-label" for="catElectronics">Electronics</label>
            </div>
            <div class="form-check mb-2">
              <input class="form-check-input" type="checkbox" value="clothing" id="catClothing" onchange="filterProducts()">
              <label class="form-check-label" for="catClothing">Clothing</label>
            </div>
            <div class="form-check mb-2">
              <input class="form-check-input" type="checkbox" value="home" id="catHome" onchange="filterProducts()">
              <label class="form-check-label" for="catHome">Home &amp; Living</label>
            </div>
            <div class="form-check mb-2">
              <input class="form-check-input" type="checkbox" value="accessories" id="catAccessories" onchange="filterProducts()">
              <label class="form-check-label" for="catAccessories">Accessories</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="checkbox" value="sports" id="catSports" onchange="filterProducts()">
              <label class="form-check-label" for="catSports">Sports</label>
            </div>
          </div>

          <!-- Price Filter -->
          <div class="mb-3">
            <h6 class="mb-2">Price Range</h6>
            <div class="d-flex gap-2">
              <div>
                <label class="form-label mb-1" style="font-size:.75rem;">Min ($)</label>
                <input type="number" id="priceMin" class="form-control form-control-sm" placeholder="0" min="0" onchange="filterProducts()">
              </div>
              <div>
                <label class="form-label mb-1" style="font-size:.75rem;">Max ($)</label>
                <input type="number" id="priceMax" class="form-control form-control-sm" placeholder="1000" min="0" onchange="filterProducts()">
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Product Grid -->
      <div class="col-lg-9">

        <!-- Results header -->
        <div class="d-flex align-items-center justify-content-between mb-3">
          <span class="results-info" id="resultsCount">Showing 12 products</span>
          <div class="d-flex align-items-center gap-2">
            <label class="form-label mb-0 results-info">Sort by:</label>
            <select class="form-select form-select-sm" style="width:auto;" id="sortSelect" onchange="filterProducts()">
              <option value="default">Default</option>
              <option value="price-asc">Price: Low to High</option>
              <option value="price-desc">Price: High to Low</option>
              <option value="name-asc">Name: A to Z</option>
              <option value="name-desc">Name: Z to A</option>
            </select>
          </div>
        </div>

        <div class="row g-3" id="productGrid">
          <!-- Products will be rendered here by JS -->
        </div>

        <!-- No Results -->
        <div id="noResults" class="text-center py-5">
          <i class="bi bi-search fs-1 text-secondary mb-3 d-block"></i>
          <h5 class="fw-bold">No products found</h5>
          <p class="text-secondary">Try adjusting your filters or search term.</p>
          <button class="btn btn-brand" onclick="resetFilters()">Clear Filters</button>
        </div>
      </div>

    </div>
  </div>

  <!-- Cart Offcanvas -->
  <div class="offcanvas offcanvas-end" tabindex="-1" id="cartOffcanvas" aria-labelledby="cartOffcanvasLabel">
    <div class="offcanvas-header border-bottom">
      <h5 class="offcanvas-title fw-bold" id="cartOffcanvasLabel">
        <i class="bi bi-cart2 me-2" style="color: var(--brand);"></i>Your Cart
      </h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body d-flex flex-column p-0">
      <!-- Empty state -->
      <div id="cartEmpty" class="text-center py-5 px-3">
        <i class="bi bi-cart-x fs-1 d-block mb-3" style="color: var(--border);"></i>
        <h6 class="fw-bold">Your cart is empty</h6>
        <p class="text-secondary" style="font-size:.875rem;">Browse products and click "Add to Cart" to get started.</p>
      </div>
      <!-- Cart items list -->
      <div id="cartItems" class="flex-grow-1 overflow-auto"></div>
      <!-- Cart footer -->
      <div id="cartFooter" class="border-top p-3" style="display:none;">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <span class="fw-bold">Total</span>
          <span class="fw-bold fs-5" style="color: var(--brand);" id="cartTotal">$0.00</span>
        </div>
        <button class="btn btn-brand w-100 mb-2">Proceed to Checkout</button>
        <button class="btn btn-outline-brand w-100" onclick="clearCart()">Clear Cart</button>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="border-top mt-5 py-4">
    <div class="container text-center">
      <small class="text-secondary">ShopCatalog &mdash; Simple Product Browsing</small>
    </div>
  </footer>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  <script src="js/productos.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    
  </script>
</body>
</html>
